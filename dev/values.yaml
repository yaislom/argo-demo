replicaCount: 3
mage:
  repository: us-central1-docker.pkg.dev/terraform-project-islom/artemis/artemis
  pullPolicy: IfNotPresent
  tag: "2.0.0"

service:
  type: ClusterIP
  port: 5000

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: dev-artemis.omahaaws.net
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  # - secretName: dev-artemis-tls
  #   hosts:
  #     - dev-artemis.omahaaws.net
